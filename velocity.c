#include<stdio.h>

int main(void)

{
    
    float distance,time,velocity;
    
	printf("Enter both distance and time: ");
	scanf(" %f" "%f",&distance, &time);
	velocity = distance/time;
	printf("your distance is %f meters, time %f and velocity = %f meters per second.",distance,time,velocity);






	return 0;

}